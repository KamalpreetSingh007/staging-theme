{%-liquid
  assign cssFile        =   'page.css' | asset_url
-%}

<link rel="stylesheet" href="{{ cssFile }}">

<div class="mainTemplate">
  <div class="page-width">
    <div class="mainTitle">
      <h1>{{ page.title }}</h1>
    </div>

    <div class="pageContent">
      {{ page.content }}
    </div>
  </div>
</div>
