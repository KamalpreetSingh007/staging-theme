window.addEventListener('load', function() {
//   $(".showCollections").each( function( i, obj ) {
//     if ( $(this).is(':in-viewport') ) {
//       const getSrc    =   $( '.showCollections__item .showCollections__img', this ).attr( 'src' );
//       //set the image src from loading to read image
//       $( '.showCollections__item .showCollections__img', this ).prop( 'testsrc', getSrc );
//     }
//   });
});